export interface ChoppOrder {
  choppId: String,
  quantity: Number
}

export interface PortionOrder {
  portionId: String,
  quantity: Number
}

export interface Chopps {
  id: string,
  weight: string
}

export interface Portions {
  id: string,
  type: string
}